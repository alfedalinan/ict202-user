type User {
    id: Int,
    firstName: String
    lastName: String
    email: String!
    role: Int
}

extend type Query {
    loginUser(
        email: String!
        password: String
    ): User
}